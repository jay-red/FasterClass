/* 1:USER:DEFAULT */
function isDown( key ) {

}

function handleKeyDown( evt ) {

}

function handleKeyUp( evt ) {

}

/* 
Assume the following are already called:

document.addEventListener( "keydown", handleKeyDown );
document.addEventListener( "keyup", handleKeyUp );
*/